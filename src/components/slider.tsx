import "@/styles/slider.scss";

const Slider = () => {
  return (
    <label className="switch">
      <input type="checkbox"/>
      <span className="slider"></span>
    </label>
  )
}

export default Slider